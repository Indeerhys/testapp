  <% if user_signed_in? %>
  <li>
    <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
  </li>
  <% end %>

<h1>Worker#index</h1>
<p>Find me in app/views/worker/index.html.erb</p>
<%= link_to("Add New Worker", {:action => 'new'}, :class => 'actions new') %>

<table>
  <% @workers.each do |w| %>
    <tr>
      <td>
        <%= w.full_name %>
        <%=link_to('Show more info.', {:action => 'show', :id => w.id}, :class => 'action show')%>
      </td>
    </tr>
  <% end %>

</table>
